<Window x:Class="RichTextControls.RichTextBoxExample"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RichTextControls"
        mc:Ignorable="d"
        Title="RichTextBoxExample" Height="350" Width="500">
    <DockPanel>
        <WrapPanel DockPanel.Dock="Top">
            <Button Name="btnGetText" Click="btnGetText_Click"  Content="GetText"/>
            <Button Name="btnSetText" Click="btnSetText_Click" Content="SetText" />
            <Button Name="btnGetSelectedText" Click="btnGetSelectedText_Click" Content="GetSelText" />
            <Button Name="btnSetSelectedText" Click="btnSetSelectedText_Click" Content="SetSelText" />
        </WrapPanel>
        <TextBox DockPanel.Dock="Bottom" Name="txtStatus" />
        <RichTextBox Name="rtbEditor" SelectionChanged="rtbEditor_SelectionChanged">
            <FlowDocument>
                <Paragraph FontSize="36">Hello world!</Paragraph>
                <Paragraph FontStyle="Italic" TextAlignment="Left" FontSize="14" Foreground="Gray">
                    Thanks to the RichTextBox control, this FlowDocument is completely editable!
                </Paragraph>
            </FlowDocument>
            
        </RichTextBox>
    </DockPanel>
</Window>
